{
  "name": "url-shortener",
  "version": "0.1.0",
  "description": "Modern serverless URL shortener using Azure and Cloudflare",
  "private": true,
  "workspaces": [
    "workers",
    "functions",
    "web",
    "landing"
  ],
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "lint": "eslint . --ext .js,.ts",
    "format": "prettier --write \"**/*.{js,ts,json,md}\""
  },
  "keywords": [
    "url-shortener",
    "azure",
    "cloudflare",
    "serverless"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "eslint": "^8.56.0",
    "jest": "^29.7.0",
    "prettier": "^3.2.0",
    "typescript": "^5.3.0"
  }
}
